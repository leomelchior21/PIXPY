export interface BossTestCase {
  inputs: string[]
  expected?: string
}

export interface VariableBoss {
  id: number
  title: string
  prompt: string
  code: string
  inputNames: string[]
  editLine: number
  testCases: BossTestCase[]
}

export const variableBosses: VariableBoss[] = [
  {
    id: 1,
    title: 'Two Numbers',
    prompt: 'Two whole numbers arrive as a and b. Replace 0 with a formula that adds them, then print the total.',
    code: 'a = int(input())\nb = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['a', 'b'],
    editLine: 4,
    testCases: [
      { inputs: ['8', '4'], expected: '12' },
      { inputs: ['2', '3'], expected: '5' },
      { inputs: ['11', '7'], expected: '18' },
      { inputs: ['0', '9'], expected: '9' },
      { inputs: ['-3', '8'], expected: '5' },
    ],
  },
  {
    id: 2,
    title: 'Difference',
    prompt: 'Find how far apart the numbers are by subtracting b from a. Store the answer in result.',
    code: 'a = int(input())\nb = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['a', 'b'],
    editLine: 4,
    testCases: [
      { inputs: ['9', '4'], expected: '5' },
      { inputs: ['15', '6'], expected: '9' },
      { inputs: ['3', '8'], expected: '-5' },
      { inputs: ['-2', '-7'], expected: '5' },
      { inputs: ['20', '3'], expected: '17' },
    ],
  },
  {
    id: 3,
    title: 'Double It',
    prompt: 'Make result twice as large as number. Keep the input and print lines exactly where they are.',
    code: 'number = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['number'],
    editLine: 3,
    testCases: [
      { inputs: ['6'], expected: '12' },
      { inputs: ['11'], expected: '22' },
      { inputs: ['-4'], expected: '-8' },
      { inputs: ['0'], expected: '0' },
      { inputs: ['21'], expected: '42' },
    ],
  },
  {
    id: 4,
    title: 'Triple It',
    prompt: 'Turn number into three times its value. Change only the formula assigned to result.',
    code: 'number = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['number'],
    editLine: 3,
    testCases: [
      { inputs: ['5'], expected: '15' },
      { inputs: ['9'], expected: '27' },
      { inputs: ['-3'], expected: '-9' },
      { inputs: ['12'], expected: '36' },
      { inputs: ['0'], expected: '0' },
    ],
  },
  {
    id: 5,
    title: 'Quotient',
    prompt: 'Divide a by b and store the quotient in result. Use Python’s division operator in your formula.',
    code: 'a = int(input())\nb = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['a', 'b'],
    editLine: 4,
    testCases: [
      { inputs: ['20', '4'], expected: '5.0' },
      { inputs: ['9', '3'], expected: '3.0' },
      { inputs: ['7', '2'], expected: '3.5' },
      { inputs: ['-12', '4'], expected: '-3.0' },
      { inputs: ['25', '5'], expected: '5.0' },
    ],
  },
  {
    id: 6,
    title: 'Remainder',
    prompt: 'After dividing a by b, store only what is left over. The % operator will help.',
    code: 'a = int(input())\nb = int(input())\n\nresult = 0  # use %\nprint(result)',
    inputNames: ['a', 'b'],
    editLine: 4,
    testCases: [
      { inputs: ['17', '5'], expected: '2' },
      { inputs: ['22', '4'], expected: '2' },
      { inputs: ['9', '7'], expected: '2' },
      { inputs: ['30', '8'], expected: '6' },
      { inputs: ['19', '6'], expected: '1' },
    ],
  },
  {
    id: 7,
    title: 'Celsius to Fahrenheit',
    prompt: 'Convert celsius using this recipe: multiply by 9, divide by 5, then add 32.',
    code: 'celsius = int(input())\n\nfahrenheit = 0  # change this\nprint(fahrenheit)',
    inputNames: ['celsius'],
    editLine: 3,
    testCases: [
      { inputs: ['20'], expected: '68.0' },
      { inputs: ['0'], expected: '32.0' },
      { inputs: ['30'], expected: '86.0' },
      { inputs: ['-10'], expected: '14.0' },
      { inputs: ['100'], expected: '212.0' },
    ],
  },
  {
    id: 8,
    title: 'Fuel Efficiency',
    prompt: 'Work out the distance traveled for each unit of fuel by dividing distance by fuel.',
    code: 'distance = int(input())\nfuel = int(input())\n\nresult = 0  # change this\nprint(result)',
    inputNames: ['distance', 'fuel'],
    editLine: 4,
    testCases: [
      { inputs: ['300', '15'], expected: '20.0' },
      { inputs: ['420', '20'], expected: '21.0' },
      { inputs: ['150', '10'], expected: '15.0' },
      { inputs: ['225', '15'], expected: '15.0' },
      { inputs: ['100', '8'], expected: '12.5' },
    ],
  },
  {
    id: 9,
    title: 'Taxi Fare',
    prompt: 'Start with base, then add distance multiplied by rate. Store the full journey cost in fare.',
    code: 'base = int(input())\ndistance = int(input())\nrate = int(input())\n\nfare = 0  # change this\nprint(fare)',
    inputNames: ['base', 'distance', 'rate'],
    editLine: 5,
    testCases: [
      { inputs: ['4', '10', '2'], expected: '24' },
      { inputs: ['5', '8', '3'], expected: '29' },
      { inputs: ['3', '12', '2'], expected: '27' },
      { inputs: ['10', '5', '4'], expected: '30' },
      { inputs: ['7', '7', '2'], expected: '21' },
    ],
  },
  {
    id: 10,
    title: 'Three Scores',
    prompt: 'Add a, b, and c, then divide their total by 3 to calculate the average score.',
    code: 'a = int(input())\nb = int(input())\nc = int(input())\n\naverage = 0  # change this\nprint(average)',
    inputNames: ['a', 'b', 'c'],
    editLine: 5,
    testCases: [
      { inputs: ['8', '9', '10'], expected: '9.0' },
      { inputs: ['10', '10', '7'], expected: '9.0' },
      { inputs: ['4', '8', '12'], expected: '8.0' },
      { inputs: ['5', '6', '7'], expected: '6.0' },
      { inputs: ['3', '9', '6'], expected: '6.0' },
    ],
  },
  {
    id: 11,
    title: 'Seconds',
    prompt: 'Split seconds into whole minutes and seconds left over. Print each answer on its own line.',
    code: 'seconds = int(input())\n\nminutes = 0  # use //\nleft = 0  # use %\nprint(minutes)\nprint(left)',
    inputNames: ['seconds'],
    editLine: 3,
    testCases: [
      { inputs: ['125'], expected: '2\n5' },
      { inputs: ['61'], expected: '1\n1' },
      { inputs: ['360'], expected: '6\n0' },
      { inputs: ['59'], expected: '0\n59' },
      { inputs: ['185'], expected: '3\n5' },
    ],
  },
  {
    id: 12,
    title: 'Weighted Score',
    prompt: 'Build one score using 40% of a and 60% of b. Remember that 40% is 0.4.',
    code: 'a = int(input())\nb = int(input())\n\nscore = 0  # change this\nprint(score)',
    inputNames: ['a', 'b'],
    editLine: 4,
    testCases: [
      { inputs: ['80', '90'], expected: '86.0' },
      { inputs: ['50', '100'], expected: '80.0' },
      { inputs: ['100', '50'], expected: '70.0' },
      { inputs: ['75', '85'], expected: '81.0' },
      { inputs: ['60', '70'], expected: '66.0' },
    ],
  },
  {
    id: 13,
    title: 'Square It',
    prompt: 'Raise number to the power of 2 with Python’s ** operator, then print the result.',
    code: 'number = int(input())\n\nresult = 0  # use **\nprint(result)',
    inputNames: ['number'],
    editLine: 3,
    testCases: [
      { inputs: ['7'], expected: '49' },
      { inputs: ['-4'], expected: '16' },
      { inputs: ['12'], expected: '144' },
      { inputs: ['0'], expected: '0' },
      { inputs: ['9'], expected: '81' },
    ],
  },
  {
    id: 14,
    title: 'Discount',
    prompt: 'Remove discount percent from price. First find the discount amount, then subtract it from price.',
    code: 'price = int(input())\ndiscount = int(input())\n\nfinal_price = 0  # change this\nprint(final_price)',
    inputNames: ['price', 'discount'],
    editLine: 4,
    testCases: [
      { inputs: ['100', '20'], expected: '80.0' },
      { inputs: ['80', '25'], expected: '60.0' },
      { inputs: ['50', '10'], expected: '45.0' },
      { inputs: ['200', '15'], expected: '170.0' },
      { inputs: ['120', '50'], expected: '60.0' },
    ],
  },
  {
    id: 15,
    title: 'Your Formula',
    prompt: 'Invent your own transformation for number. Every test must print a result, but the rule is yours.',
    code: 'number = int(input())\n\nresult = number * 2  # make it yours\nprint(result)',
    inputNames: ['number'],
    editLine: 3,
    testCases: [
      { inputs: ['7'] },
      { inputs: ['3'] },
      { inputs: ['12'] },
      { inputs: ['-2'] },
      { inputs: ['21'] },
    ],
  },
]
